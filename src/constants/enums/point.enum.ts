export enum PointType {
  CLASSIC = 'CLASSIC',
  PERCENTAGE = 'PERCENTAGE',
}

export enum FixedPoints {
  Bronze = 5,
  Sliver = 10,
  Gold = 15,
}

export enum FixedRanks {
  Bronze = 0,
  Sliver = 2000,
  Gold = 5000,
}

export enum RankId {
  Bronze = 1,
  Sliver = 2,
  Gold = 3,
}
